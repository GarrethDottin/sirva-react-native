import colors from '../Theme/Colors';

export default {
    cardShadow: {
        shadowColor: colors.black,
        shadowOffset: { width: 0, height: 8 },
        shadowOpacity: 0.1,
        shadowRadius: 8,
    }
}
