import { StyleSheet } from 'react-native'
import { Variables } from '../../../Theme'

export default htmlStyles = StyleSheet.create({
    b: {
        fontWeight: 'bold',
    },
    strong: {
        fontWeight: 'bold',
    },
    p: {
        marginBottom: -50,
    },
    ol: {
        marginLeft: Variables.smallGutter,
        marginBottom: -50,
    },
})