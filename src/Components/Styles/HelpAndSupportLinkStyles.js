import { StyleSheet } from 'react-native'
import { Variables } from '../../Theme'

export default StyleSheet.create({
    supportLink: {
        position: 'absolute',
        top: 20,
        right: 0,
        width: 200,
        marginRight: 10,
        height: 80,
        zIndex: 9999,
        alignItems: 'flex-end'
    }
})
