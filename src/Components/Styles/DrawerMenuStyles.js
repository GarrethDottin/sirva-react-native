import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  menuContainer: {
    width: "100%",
    alignItems: "center",
    justifyContent: "center"
  },
  buttonContainer: {
    padding: 10,
    alignItems: "center",
    justifyContent: "center"
  },
  image: {
    width: 16,
    height: 12
  }
})